# Implementation Plans

Generated by the improve skill on 2026-06-22. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore the web verification baseline | P1 | S | - | TODO |
| 002 | Split the admin app into maintainable screen modules | P1 | M | 001 | TODO |
| 003 | Productionize the content approval desk with shadcn app primitives | P1 | M | 001, 002 | TODO |
| 004 | Reconcile Select and Drawer behavior with shadcn Base UI patterns | P2 | M | 001 | TODO |

## Dependency notes

- 001 should land first because the current web lint gate fails on unused symbols; later refactors need clean lint/typecheck/build gates.
- 002 should land before 003 because content desk UI work is easier and safer once `ContentView` is isolated from dashboard, RAG, and chat lab code.
- 003 can use the isolated content module from 002 to add production shadcn primitives without expanding the already-large `App.tsx`.
- 004 can happen after 001 independently, but it should coordinate with 003 if both touch the content editor overlay.

## Findings considered and rejected

- Full app-wide `Sidebar` conversion: deferred. The content desk is the user-visible pain point; install and use `Table`, `Sheet`/`Drawer`, `Tooltip`, `Sonner`, and `Skeleton` first, then consider `Sidebar` as a separate app-shell plan.
- Backend/API pagination for content inventory: not planned yet. The current dataset is small enough that the first production UI improvement can use existing `/admin/content` data.
- Production authentication: acknowledged but out of scope for this UI/UX shadcn pass. Existing API code explicitly treats the admin stub as development-only.
