# Implementation Plans

Generated by the improve skill on 2026-06-22. Plan 005 added 2026-06-23 for Epic A (real RAG). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, web-search official docs per segment (see `AGENTS.md`), and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore the web verification baseline | P1 | S | - | DONE |
| 002 | Split the admin app into maintainable screen modules | P1 | M | 001 | DONE |
| 003 | Productionize the content approval desk with shadcn app primitives | P1 | M | 001, 002 | DONE |
| 004 | Reconcile Select and Drawer behavior with shadcn Base UI patterns | P2 | M | 001 | DONE |
| 005 | Real RAG platform pipeline (embeddings + pgvector + async worker) | P1 | L | - | DONE |
| 006 | RAG embedder service + Docker E2E pipeline | P1 | M | 005 | DONE |
| 007 | Knowledge sync UX + BGE-M3 compose profile | P1 | M | 006 | DONE |
| 008 | Production hardening (readiness, healthchecks, deploy docs) | P1 | M | 007 | DONE |
| 009 | Widget polish (origin-check, policy UI, rate-limit UX) | P1 | M | 008 | DONE |
| 010 | BGE-M3 smoke scripts + docker validation | P2 | M | 007 | DONE |
| 011 | Meta webhooks + live replies (WhatsApp + Instagram) | P2 | M | 008 | DONE |
| 012 | Per-project AI keys (LLM + embeddings) | P1 | M | 005 | DONE |
| 013 | Better Auth production admin | P1 | M | 008 | DONE |

## Dependency notes

- Plans 001–004 (web UI / shadcn) are complete as of 2026-06-23.
- Plan 005 (real RAG) is DONE as of 2026-06-23.
- Plan 006 (embedder + Docker E2E) is DONE as of 2026-06-23 — `apps/rag-embedder`, compose, `pnpm smoke:rag-pipeline`.
- Plan 007 (knowledge sync UX + BGE-M3 profile) is DONE as of 2026-06-23 — admin polling, `docker-compose.bge-m3.yml`.
- Plans 008–013 (hardening, widget polish, BGE smoke, Meta live replies, per-project AI keys, Better Auth) are DONE as of 2026-06-23.
- `App.tsx` is a ~330-line shell; admin surfaces live under `apps/web/src/features/admin/`.
- `ContentView` uses Table, Skeleton, Sonner toasts, and right-side Drawers.
- Platform RAG: async `rag.index` worker + pgvector retrieval; stub embedder default in phase0 compose.

## Findings considered and rejected

- Full app-wide `Sidebar` conversion: deferred. The content desk is the user-visible pain point; install and use `Table`, `Sheet`/`Drawer`, `Tooltip`, `Sonner`, and `Skeleton` first, then consider `Sidebar` as a separate app-shell plan.
- Backend/API pagination for content inventory: not planned yet. The current dataset is small enough that the first production UI improvement can use existing platform content APIs.
- Production authentication: acknowledged but out of scope for the UI/UX shadcn pass (001–004). Plan 005 does not include Better Auth.
- Building the `rag-embedder` Docker image in plan 005: deferred to plan 006 (now DONE; stub default in phase0 compose).